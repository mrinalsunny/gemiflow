# hive-mind

Hive Mind collective intelligence system for advanced swarm coordination.

## Usage
```bash
npx gemiflow4gemini hive-mind [subcommand] [options]
```

## Subcommands
- `init` - Initialize hive mind system
- `spawn` - Spawn hive mind swarm
- `status` - Show hive mind status
- `resume` - Resume paused session
- `stop` - Stop running session

## Examples
```bash
# Initialize hive mind
npx gemiflow4gemini hive-mind init

# Spawn swarm
npx gemiflow4gemini hive-mind spawn "Build microservices"

# Check status
npx gemiflow4gemini hive-mind status
```
